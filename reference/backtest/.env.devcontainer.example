# Overrides used when running app/worker directly inside VS Code dev container.
POSTGRES_DSN=postgresql+psycopg://backtest:backtest@host.docker.internal:5432/backtest
REDIS_URL=redis://host.docker.internal:6379/0
DUCKDB_PATH=./data/datasets/market.duckdb
WATCH_DIR=./data/watch
ENABLE_WATCHER=false
